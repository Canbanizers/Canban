<div class="profile">
	<div>
		<form>
			<button {{action "cancel"}}> Back to Board </button>
			<button {{action "delete"}}>Delete</button>

			<div>
				<label>Firstname</label>
				{{view App.ValidationTextField viewName="firstName" valueBinding="firstName"}}
				<span>
					{{#if firstNameError}}
						{{view App.IconButtonView type='remove'}}
						<span class="error-message">Please fill in.</span>
					{{/if}}
				</span>
			</div>

			<div>
				<label>Lastname</label>
				{{view App.ValidationTextField viewName="lastName" valueBinding="lastName"}}
				<span>
					{{#if lastNameError}}
						{{view App.IconButtonView type='remove'}}
						<span class="error-message">Please fill in.</span>
					{{/if}}
				</span>
			</div>

			<div>
				<label>Email</label>
				{{view App.ValidationTextField disabled="disabled" viewName="email" valueBinding="email"}}
			</div>

			<div>
				<label>Password</label>
				{{view App.ValidationTextField type="password" viewName="password" valueBinding="password"}}
				<span>
					{{#if passwordError}}
						{{view App.IconButtonView type='remove'}}
						<span class="error-message">Please fill in.</span>
					{{/if}}
				</span>
			</div>

			<div>
				<label>Password Confirmation</label>
				{{view App.ValidationTextField type="password" viewName="passwordConfirmation" valueBinding="passwordConfirmation" }}
				<span>
					{{#if passwordConfirmationError}}
						{{view App.IconButtonView type='remove'}}
						<span class="error-message">Please confirm your password.</span>
					{{else}}
						{{#if passwordConfirmationCompareError}}
							{{view App.IconButtonView type='remove'}}
							<span class="error-message">Passwords don't match.</span>
						{{/if}}
					{{/if}}
				</span>
			</div>

		<button {{action "save"}}> Save changes </button>
		<button {{action "cancel"}}> Cancel </button>
		</form>
	</div>
</div>