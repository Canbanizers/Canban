#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-ember-handlebars/bin/grunt-ember-handlebars" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-ember-handlebars/bin/grunt-ember-handlebars" "$@"
  ret=$?
fi
exit $ret
